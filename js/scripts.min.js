var releasesource=$("#release-template").html(),releasetemplate=Handlebars.compile(releasesource);$(".releases").html(releasetemplate(releases));var option="all",releaseRow=$(".release-row");$("#options a").click(function(){option=$(this).attr("data-option");$("#options li").removeClass("active");$(this).parent().attr("class","active");releaseRow.each(function(){$(this).show()});if(option==="all")return;releaseRow.each(function(){$(this).attr("data-type")!==option&&$(this).hide()})});